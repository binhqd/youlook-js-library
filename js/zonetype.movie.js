/**/
// Override config
zone.zonetype.config = $.extend({}, zone.zonetype.config, {
	homeURI : 'movie'
});

// Class
;(function($, scope){
	scope['movie'] = {
		
	};
})(jQuery, zone['zonetype']);

$(document).ready(function() {
	
});